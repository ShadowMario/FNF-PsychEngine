package gamejolt;

class GJKeys
{
    /*
        Put your game data here, but do NOT make it public.
        To avoid this, when it's about be uploaded to GitHub,
        go to the ".gitignore" file and add the line "source/gamejolt/GJKeys.hx"
        so GitHub will ignore that file when uploading :)
    */

    public static var id:Int = 0;
    public static var key:String = '';
}