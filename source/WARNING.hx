package;

/* in case someone edits this and fixes it

import.flixel.FlxSprite;
import.flixel.FlxButton;
import.flixel.FlxText;

*/

using StringTools;

class ERROR! {
public static var WARNING:Array<Dynamic> = [
  ["Hey, i see that your pc is running a bit slow! Might wanna watch out for some bugs!"],
  ["I recommend you to stop playing and fix some fps issues!"]
  
  
  // adding a smol detector
  
  if ( FPS == 30 )
  {
   add(WARNING);
  }
  else
  {
   kill(WARNING);
  }
  // kinda don't add a FlxButton cuz i'm lazy
    ]
}
